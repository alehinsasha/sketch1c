
&НаКлиенте
Процедура ПрименитьНастройкиПроекта(Команда)
	ИсходнаяСтруктура = Новый Структура;
	
	ИсходнаяСтруктура.Вставить("ВключитьРежимРедактированияСвойств", Объект.ВключитьРежимРедактированияСвойств);
	
	Результат = Новый ФиксированнаяСтруктура(ИсходнаяСтруктура);

	Закрыть(Результат);
	
КонецПроцедуры
